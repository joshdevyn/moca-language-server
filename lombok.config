# Lombok configuration file
# Exclude ANTLR-generated files from processing
lombok.addLombokGeneratedAnnotation = true
config.stopBubbling = true

# Exclude specific directories from lombok processing
lombok.extern.findbugs.addSuppressFBWarnings = true

# Add generated annotation to exclude from static analysis
lombok.addGeneratedAnnotation = true
